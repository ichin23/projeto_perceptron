---
phase: 2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - perceptron.py
  - test_perceptron.py
autonomous: true
requirements:
  - ALGO-01
  - ALGO-02
  - ALGO-03
  - ALGO-04
---

# Plan: Phase 2 - Algorithm Implementation

Implement the core Perceptron algorithm and its supervised Hebbian training rule.

<objective>
Create the `Perceptron` class in `perceptron.py` and verify its logic with unit tests.
</objective>

<tasks>
  <task type="execute">
    <files>
      <file>perceptron.py</file>
    </files>
    <action>
      Implement the `Perceptron` class.
      - `__init__(self, learning_rate=0.01, initial_weights=None)`: Store eta and weights.
      - `predict(self, x)`: Compute $u = w_0 x_0 + \sum w_i x_i$ where $x_0 = -1$. Apply sign activation.
      - `train_step(self, x, d)`: Update weights using $w = w + \eta (d - y) x$.
    </action>
    <verify>
      Check for syntax errors with `python3 -m py_compile perceptron.py`.
    </verify>
    <acceptance_criteria>
      File exists and contains the required methods.
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>test_perceptron.py</file>
    </files>
    <read_first>
      <file>perceptron.py</file>
    </read_first>
    <action>
      Create unit tests for the Perceptron:
      - Test `predict` with known weights.
      - Test `train_step` ensuring weights change correctly for a single update.
    </action>
    <verify>
      python3 test_perceptron.py
    </verify>
    <acceptance_criteria>
      All unit tests pass.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run `python3 test_perceptron.py` to ensure the mathematical logic follows the Supervised Hebbian Rule.
</verification>

<success_criteria>
- [ ] `perceptron.py` implements the class with bias $x_0 = -1$.
- [ ] `train_step` correctly implements the Hebbian update.
- [ ] Unit tests confirm behavioral correctness.
</success_criteria>

## PLANNING COMPLETE
