---
phase: 1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - training_data.csv
  - load_data.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
---

# Plan: Phase 1 - Setup & Data

Prepare the environment by creating the training data file and a verification script for data ingestion.

<objective>
Create `training_data.csv` and a `load_data.py` script to verify NumPy can correctly load the 30 patterns.
</objective>

<tasks>
  <task type="execute">
    <files>
      <file>training_data.csv</file>
    </files>
    <read_first>
      <file>.planning/PROJECT.md</file>
    </read_first>
    <action>
      Create `training_data.csv` with the 30 patterns provided in the project context.
      Columns: x1, x2, x3, d
    </action>
    <verify>
      cat training_data.csv | wc -l
    </verify>
    <acceptance_criteria>
      File contains 30 lines of data (plus optional header).
    </acceptance_criteria>
  </task>

  <task type="execute">
    <files>
      <file>load_data.py</file>
    </files>
    <read_first>
      <file>training_data.csv</file>
    </read_first>
    <action>
      Create `load_data.py` that imports numpy and uses `np.genfromtxt` or `np.loadtxt` to load `training_data.csv`.
      It should print the shape of the features and targets.
    </action>
    <verify>
      python3 load_data.py
    </verify>
    <acceptance_criteria>
      Script exits 0 and prints shape (30, 4) or similar.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
1. Run `python3 load_data.py` and ensure it correctly loads all 30 patterns.
</verification>

<success_criteria>
- [ ] `training_data.csv` exists with 30 patterns.
- [ ] `load_data.py` successfully loads the data using NumPy.
</success_criteria>

## PLANNING COMPLETE
