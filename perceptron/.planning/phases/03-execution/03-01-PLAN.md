---
phase: 3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - train.py
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-02
  - TRAIN-03
  - TRAIN-04
  - VIS-01
  - REPORT-01
  - TEST-01
---

# Plan: Phase 3 - Execution & Visualization

Implement the full training cycle and visualization logic for the oil purity classification.

<objective>
Create `train.py` to run 5 independent training sessions and generate epoch-based performance graphs.
</objective>

<tasks>
  <task type="execute">
    <files>
      <file>train.py</file>
    </files>
    <read_first>
      <file>perceptron.py</file>
      <file>load_data.py</file>
    </read_first>
    <action>
      Implement `train.py`:
      - Load data using `load_data.load_training_data()`.
      - Loop 5 times (independent runs).
      - For each run:
        - Initialize `Perceptron` with random weights [0, 1].
        - Loop through epochs (max 1000).
        - In each epoch, iterate over all 30 patterns and call `train_step`.
        - Count total errors per epoch.
        - Stop if error count is 0.
        - Log final weights, epochs, and final error.
      - Classification Test:
        - After training each run, classify the 10 samples provided by the user.
      - Report Generation:
        - Generate `report.html` and `report.css`.
        - Use `matplotlib` to save error plots as PNGs and embed them.
        - Populate tables with training results (weights/epochs) and classification results (T1-T5).
        - Include the answers to the two theoretical questions.
    </action>
    <verify>
      .venv/bin/python3 train.py
    </verify>
    <acceptance_criteria>
      Script runs to completion, reports 5 runs, and saves/shows graphs.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Ensure `train.py` correctly handles the randomized weight requirement and stops appropriately at the convergence or epoch limit.
</verification>

<success_criteria>
- [ ] 5 independent runs are executed.
- [ ] Initial weights are random [0, 1].
- [ ] Convergence (0 errors) or 1000 epoch limit is honored.
- [ ] Graphs are generated and embedded in a web report.
- [ ] Classification of 10 test samples is performed across all 5 runs.
- [ ] Web report includes training results table and answers to theoretical questions.
</success_criteria>

## PLANNING COMPLETE
